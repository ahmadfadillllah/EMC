<!DOCTYPE html>
<html lang="id">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Halaman Integrasi API</title>
    <style>
        /* Mengatur background halaman */
        body {
            font-family: 'Arial', sans-serif;
            background: linear-gradient(135deg, #0b3c40, #188e99);
            margin: 0;
            padding: 0;
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
            color: white;
        }

        /* Kontainer utama untuk teks */
        .container {
            text-align: center;
            background-color: rgba(255, 255, 255, 0.1);
            padding: 50px;
            border-radius: 15px;
            box-shadow: 0 8px 16px rgba(0, 0, 0, 0.3);
            width: 80%;
            max-width: 600px;
        }

        /* Judul yang besar dan menarik */
        h1 {
            font-size: 3em;
            font-weight: bold;
            letter-spacing: 2px;
            text-shadow: 2px 2px 5px rgba(0, 0, 0, 0.5);
        }

        /* Deskripsi yang lebih kecil dengan sentuhan artistik */
        p {
            font-size: 1.2em;
            margin-top: 20px;
            font-weight: 300;
            color: #f4f4f4;
        }

        /* Efek animasi untuk container */
        .container {
            animation: fadeIn 2s ease-in-out;
        }

        /* Animasi fade-in */
        @keyframes fadeIn {
            0% { opacity: 0; }
            100% { opacity: 1; }
        }
    </style>
</head>
<body>

    <div class="container">
        <h1>Hello World</h1>
    </div>

</body>
</html>
